using System;
using SystemReport;
using UnityEngine;

namespace Avatar
{
    public class AvatarManager : MonoBehaviour
    {
        private void Update()
        {
            /*
            for (int i = 0; i < CamerasManager.joints.length; i = i + 3)
            {
                BodyCoordinates.GetInstance().UpdateBodyCoordinates(new BodyCoordinates.KeyValue(
                    ((EAvatarBones)(i / 3)),
                    new Vector3(CamerasManager.joints[i], CamerasManager.joints[i + 1], CamerasManager.joints[i + 2])));
            } //end-for
            */
        }
    }
}