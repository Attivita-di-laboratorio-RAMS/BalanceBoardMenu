using UnityEngine;

public class Rotate : MonoBehaviour{
    [SerializeField] private Transform _avatar;
    
    // Update is called once per frame
    // void Update(){
    //     _avatar.Rotate(0, 0.1f, 0, Space.Self);
    // }
}
